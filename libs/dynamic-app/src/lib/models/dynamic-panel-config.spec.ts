import { DynamicPanelConfig } from './dynamic-panel-config';

describe('DynamicPanelConfig', () => {
  it('should create an instance', () => {
    expect(new DynamicPanelConfig()).toBeTruthy();
  });
});
